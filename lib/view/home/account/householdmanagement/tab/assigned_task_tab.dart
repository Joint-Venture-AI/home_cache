import 'package:flutter/material.dart';

class AssignedTaskTab extends StatelessWidget {
  const AssignedTaskTab({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(children: [
       
      ],
    );
  }
}
